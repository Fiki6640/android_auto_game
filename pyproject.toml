[project]
name = "android-bot"
version = "1.0.2"
description = "WiFi ADB + OpenCV 截图匹配 + 自动点击"
requires-python = ">=3.10"
dependencies = [
    "opencv-python-headless",
    "numpy",
    "pyyaml",
    "rapidocr-onnxruntime",
    "PySide6",
]


